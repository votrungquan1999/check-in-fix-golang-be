package utils

import (
	"firebase.google.com/go/v4/auth"
	"github.com/gin-gonic/gin"
)

func GetCurrentEmployer(c *gin.Context) (*auth.UserRecord, error) {
	//user, _ := c.Get("user")

	//empl

	return nil, nil
}
